# macOS
.DS_Store

# Python
venv_legion/
.venv/
__pycache__/
*.pyc
*.egg-info/

# Node.js
# If node_modules is at the root, 'node_modules/' is fine.
# If it can appear in subdirectories (like inside computer_use_mcp), use '**/node_modules/'
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# package-lock.json # Keep commented if you INTEND to track package-lock.json files.
                  # If you want to ignore one specifically: mcp_servers/computer_use_mcp/package-lock.json
                  # If you want to ignore ALL (less common for committed code): **/package-lock.json

# General
.env
.roomodes
logs/ # This correctly ignores the top-level 'logs' directory and all its contents

# IDE/Editor specific
.vscode/
.idea/